@model MVC_Project.Models.VoteRecord



<!DOCTYPE html>
<html lang="en">
<head>
    <link href="~/css/SelectDate.css" rel="stylesheet">
    <!-- BAR -->
    <title>JO!N</title>
    <link rel="icon" href="~/IMG/小黃人.png" type="image/png" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

</head>

<body>


    <!-- 帳密   -->
    <div id="container1">
        <form asp-action="Create" asp-controller="VoteRecords" method="post" class="login" id="login">
            <div class="header1">
                <h1>您好,親愛的會員</h1>
                <p style="font-size: 21px;">當您在選擇日期截止日之前，都可以編輯您最適合的時間。</p>
                <p style="font-size: 21px;">選擇日期截止後，系統將根據最多人選擇的時間段來安排活動。</p>
                <p style="font-size: 21px;">如果最終活動時間與您所選的時間不符，您將無法參加活動。</p>
                <p style="font-size: 21px;">但您可以再次考慮時間，然後再決定是否參加。</p>
                <div class="container">
                    @if (ViewBag.Dates != null)
                    {
                        foreach (var date in ViewBag.Dates)
                        {
                            <li class="Cli">
                                <input type="radio" id="@date" name="VoteResult" value="@date">
                                <label for="@date">@date</label>
                                <div class="check"></div>
                            </li>
                        }
                    }
                    else
                    {
                        <li>沒有可用的日期</li>
                    }
                </div>
            </div>
            <!-- 添加按鈕容器 -->
            <div class="button-container">
                <button class="button" type="submit" style="font-size: 20px;" value="Create">提交</button>
            </div>
            <div class="register-link">
                <a asp-controller="Member" asp-action="Registration">
                    <span onclick="show_hide()">返回會員中心</span>
                </a>
            </div>
        </form>
    </div>





</body>

<!-- bar-js -->
<script src="~/js/SelectDate.js"></script>

</html>